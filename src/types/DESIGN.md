# types design

- Purpose: Shared DTOs and types used by public APIs.
- Scope: Type declarations only; no runtime logic.
- Output: JavaScript is emitted to `dist/`; keep this directory TypeScript-only.
